<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <!-- ============================= -->
    <!-- VUE LISTE DES ADHÉSIONS -->
    <!-- ============================= -->

    <record id="view_tontine_subscription_list" model="ir.ui.view">
        <field name="name">tontine.subscription.list</field>
        <field name="model">tontine.subscription</field>

        <field name="arch" type="xml">
            <list string="Adhésions">
                <field name="member_id"/>
                <field name="group_id"/>
                <field name="order"/>
                <field name="join_date"/>
            </list>
        </field>
    </record>

    <!-- ============================= -->
    <!-- VUE FORMULAIRE -->
    <!-- ============================= -->

    <record id="view_tontine_subscription_form" model="ir.ui.view">
        <field name="name">tontine.subscription.form</field>
        <field name="model">tontine.subscription</field>

        <field name="arch" type="xml">
            <form string="Adhésion">

                <sheet>

                    <group>
                        <group string="Informations">
                            <field name="group_id" domain="[('state', '=', 'draft')]"/>
                            <field name="available_member_ids" invisible="1"/>
                            <field name="member_id" domain="[('id', 'in', available_member_ids)]" readonly="not group_id"/>
                        </group>
                        <group string="Participation">
                            <field name="order" readonly="1"/>
                            <field name="join_date"/>
                        </group>
                    </group>

                </sheet>

            </form>
        </field>
    </record>

    <record id="view_tontine_subscription_kanban" model="ir.ui.view">
        <field name="name">tontine.subscription.kanban</field>
        <field name="model">tontine.subscription</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="group_id">
                <field name="member_id"/>
                <field name="group_id"/>
                <field name="order"/>
                <field name="join_date"/>
                <field name="payment_status"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="member_id"/>
                                    </strong>
                                    <span class="text-muted">
                                        <field name="group_id"/>
                                    </span>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div>
                                    <i class="fa fa-sort-numeric-asc"/> Ordre : <field name="order"/>
                                </div>
                                <div>
                                    <i class="fa fa-calendar"/> <field name="join_date"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <field name="payment_status" widget="label_selection"
                                        options="{'classes': {'paid': 'success', 'late': 'danger', 'none': 'default'}}"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <!-- ============================= -->
    <!-- ACTION -->
    <!-- ============================= -->

    <record id="action_tontine_subscription" model="ir.actions.act_window">
        <field name="name">Adhésions</field>
        <field name="res_model">tontine.subscription</field>
        <field name="view_mode">kanban,list,form</field>
    </record>

</odoo>